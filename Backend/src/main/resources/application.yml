spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Diagramador
    username: postgres
    password: Ch@rles7i02s6q5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080

gemini:
  api-key: ${GEMINI_API_KEY:}
  model: gemini-3.6-flash